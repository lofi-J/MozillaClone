import SectionSplit from "../components/section-split/SectionSplit";
import MozillaIntro from "../components/mozilla-intro/MozillaIntro";

function Main() {
    return (
        <div>
            <SectionSplit />
            <MozillaIntro />
        </div>
    );
}

export default Main;